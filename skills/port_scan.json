{
  "type": "function",
  "function": {
    "name": "port_scan",
    "description": "Scan local/remote host ports quickly",
    "parameters": {
      "type": "object",
      "properties": {
        "host": {
          "type": "string",
          "desc": "IP address to scan"
        },
        "port_range": {
          "type": "string",
          "desc": "Ports range (e.g. '80-100' or single '80')"
        }
      },
      "required": [
        "host",
        "port_range"
      ]
    }
  }
}